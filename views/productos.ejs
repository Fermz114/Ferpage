<%- include('layouts/header.ejs') -%>

<div class="container">
    <%= title %>
    <!-- Leer la información de la Base de datos -->
    <div class="row">
        <% for(var i = 0; i < Guantes.length; i++) { %>

        <div class="col-sm-3">
            <div class="card">
                <img height="400" src="<%= Guantes[i].imagen %>" class="card-imagen">
                <div class="card-body">
                    <h5 class="card-title"><%= Guantes[i].titulo %></h5>
                    <p class="card-text"><%= Guantes[i].descripcion %></p>
                    <p class="card-text">Precio: $<%= Guantes[i].precio %></p>
                    <a href="#" class="btn btn-primary">Comprar</a>
                </div>
            </div>
        </div>

        <% } %>
    </div>
</div>

<%- include('layouts/footer.ejs') -%>
 